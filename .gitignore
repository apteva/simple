node_modules/
*.log
.DS_Store
# Bundler output — `bun run build` regenerates this; the embed
# pipeline (apteva-server's Dockerfile / scripts/build-local.sh) reads
# from a fresh build, never from a committed dist.
dist/
# Release tarballs — built by CI per release and uploaded as GitHub
# release assets. Never committed.
release/
